/target
**/*.rs.bk

# Live smoke-test output (transcript, per-case captures, results.csv, coverage).
# `smoke-results` is what CI passes via -OutDir; `winlsof-smoke-results` is the
# harness's default when it's run by hand.
/smoke-results/
/winlsof-smoke-results/
